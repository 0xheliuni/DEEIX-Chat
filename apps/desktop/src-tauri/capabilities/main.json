{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capabilities granted to the main window. Deliberately minimal: keychain access for the refresh token, the updater and relaunch, and opening http(s) URLs in the system browser for OAuth. No shell, no filesystem, no HTTP.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-focus",
    "core:webview:allow-internal-toggle-devtools",
    "updater:default",
    "process:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://*"
        },
        {
          "url": "http://*"
        }
      ]
    }
  ]
}
