{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capabilities granted to the main window. Deliberately minimal: session commands (store once / refresh / clear \u2014 the refresh token is never readable), the updater and relaunch, and opening http(s) URLs in the system browser for OAuth. No shell, no filesystem, no HTTP plugin.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-focus",
    "updater:default",
    "process:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://*"
        },
        {
          "url": "http://*"
        }
      ]
    }
  ]
}
