# DEEIX Chat 用户使用指南

## 目录

- [1. 快速上手](#1-快速上手)
- [2. 注册与登录](#2-注册与登录)
- [3. 对话功能](#3-对话功能)
- [4. 文件管理](#4-文件管理)
- [5. 用户设置](#5-用户设置)
- [6. 订阅与计费](#6-订阅与计费)
- [7. 常见问题](#7-常见问题)

---

## 1. 快速上手

### 1.1 访问系统

在浏览器中打开管理员提供的 DEEIX Chat 地址（例如 `https://modex-ai.modex-ai.cloud`）。

### 1.2 首次使用流程

```
注册/登录 → 开始新对话 → 选择模型 → 发送消息 → 查看回复
```

---

## 2. 注册与登录

### 2.1 注册账号

1. 在登录页面点击 **"注册"** 链接
2. 输入邮箱地址和密码
3. 如果管理员启用了邮箱验证，需输入收到的验证码
4. 如果启用了 Cloudflare Turnstile 人机验证，需完成验证
5. 点击注册完成

### 2.2 登录

1. 在登录页面输入账号和密码
2. 点击 **"登录"**
3. 首次登录会要求修改初始密码（管理员分配的账号）

### 2.3 安全功能

- **2FA / TOTP 双因素认证**：在账户设置中可开启，使用 Google Authenticator 等应用扫码绑定
- **可信设备**：开启 2FA 后可标记当前设备为可信设备，后续登录免二次验证
- **SSO 登录**：如果管理员配置了 OAuth/OIDC 身份源，可使用第三方账号登录（如 Google、GitHub 等）

### 2.4 忘记密码

1. 在登录页面点击 **"忘记密码"**
2. 输入注册邮箱
3. 查收邮件中的重置链接
4. 设置新密码

---

## 3. 对话功能

### 3.1 开始新对话

1. 点击左侧边栏的 **"新对话"** 按钮
2. 在输入框中输入消息
3. 按 **Enter** 发送（Shift+Enter 换行）

### 3.2 选择模型

在对话输入框上方，点击模型选择器，可选择不同的 AI 模型。可用模型由管理员配置。

### 3.3 对话操作

| 操作 | 说明 |
|------|------|
| **编辑消息** | 点击消息旁的编辑按钮，修改后重新发送 |
| **重试** | 对不满意的回复点击重试，重新生成 |
| **分支** | 从某条消息创建新分支，探索不同回答方向 |
| **反馈** | 对回复点赞/点踩，帮助改进 |
| **分享** | 生成对话分享链接（需管理员开启） |

### 3.4 多模态输入

- **图片**：拖拽或点击附件按钮上传图片，支持图片理解
- **文件**：上传文档文件（PDF、Word、Excel 等），系统会自动提取文本内容
- **富文本**：支持 Markdown、LaTeX 公式（KaTeX）、代码高亮

### 3.5 模型参数配置

点击输入框旁的 **参数配置** 按钮，可调整：

| 参数 | 说明 |
|------|------|
| **Temperature** | 控制回复的随机性（0=确定性，1=创造性） |
| **Max Tokens** | 限制回复的最大长度 |
| **Top P** | 核采样参数 |
| **系统提示词** | 自定义系统级指令 |

> 注意：部分参数可能因管理员的锁定策略而不可修改。

### 3.6 工具与 MCP

如果管理员配置了 MCP 工具或官方原生工具，在对话中：

1. 点击工具选择按钮
2. 勾选需要使用的工具
3. 发送消息后，AI 会自动调用工具并展示结果

工具调用过程会在消息中展示为 **"工具链路"**，可查看每一步的调用详情。

### 3.7 消息处理轨迹

每条 AI 回复下方可查看处理轨迹，包含：

- **处理链路**：文件预处理、全文注入、RAG 检索、上下文压缩等
- **思考链路**：模型的推理/思考过程（如果模型支持）
- **工具链路**：MCP 工具调用与结果

### 3.8 会话管理

- **最近会话**：点击左侧边栏 **"最近会话"** 查看所有历史对话
- **搜索会话**：在会话列表中搜索关键词
- **删除会话**：右键或点击菜单删除不需要的会话

---

## 4. 文件管理

### 4.1 上传文件

1. 进入 **"文件"** 页面
2. 点击 **"上传"** 按钮或拖拽文件到上传区域
3. 支持批量上传

### 4.2 文件处理

上传后系统会自动处理文件：

| 文件类型 | 处理方式 |
|----------|----------|
| **图片** | 可直接在对话中使用，或进行 OCR 提取文字 |
| **文本文件** | 小文件全文注入对话，大文件走 RAG 检索 |
| **PDF/Office** | 通过提取引擎（Tika/Docling/MinerU）提取文本 |

### 4.3 文件操作

- **预览**：点击文件卡片预览内容
- **删除**：单个删除或批量选择后删除
- **存储配额**：页面顶部显示已用/总额度

### 4.4 在对话中使用文件

1. 在对话输入框点击附件按钮
2. 选择已上传的文件
3. 文件内容会自动注入对话上下文

---

## 5. 用户设置

进入 **"设置"** 页面，包含以下选项卡：

### 5.1 通用设置

- **语言**：界面语言选择
- **主题**：浅色/深色/跟随系统
- **其他偏好**

### 5.2 对话设置

- **默认模型**：设置新对话的默认模型
- **消息窗口大小**：控制发送给模型的上下文长度
- **Token 预算**：限制单次对话的 Token 消耗
- **上下文压缩**：开启后超长对话会自动压缩摘要

### 5.3 订阅与用量

- **当前订阅**：查看订阅计划和到期时间
- **用量统计**：查看 Token 消耗、API 调用次数等
- **充值记录**：查看历史充值和消费明细

### 5.4 账户设置

- **修改密码**
- **绑定邮箱/手机**
- **2FA 双因素认证**：开启/关闭
- **SSO 身份源**：查看和管理关联的第三方账号
- **会话管理**：查看和注销其他设备的登录会话

### 5.5 关于

- 当前版本信息
- 检查更新

---

## 6. 订阅与计费

### 6.1 计费模式

DEEIX Chat 支持多种计费模式（由管理员配置）：

| 模式 | 说明 |
|------|------|
| **按量计费** | 按实际使用的 Token 数量计费 |
| **订阅制** | 按月/年订阅，包含一定用量 |
| **充值余额** | 预充值，按量扣费 |

### 6.2 支付方式

- **Stripe**：支持国际信用卡
- **易支付（EPay）**：支持国内支付方式

### 6.3 查看账单

在 **"设置 → 订阅与用量"** 中可查看：

- 当前余额
- 本月用量趋势图
- 详细消费记录

---

## 7. 常见问题

### Q: 对话没有回复怎么办？

1. 检查网络连接
2. 确认模型是否可用（联系管理员）
3. 尝试切换其他模型
4. 检查是否超出用量限额

### Q: 文件上传失败？

1. 检查文件大小是否超出限制
2. 检查存储配额是否已满
3. 确认文件格式是否支持

### Q: 如何导出对话？

目前支持通过 **"分享"** 功能生成对话链接。如需完整导出，请联系管理员。

### Q: 模型回复质量不好？

1. 尝试调整 Temperature 参数
2. 使用更精确的提示词
3. 切换到更强大的模型
4. 在对话中提供更多上下文

---

> 更多帮助请联系系统管理员或访问 [DEEIX 官网](https://deeix.com)。
